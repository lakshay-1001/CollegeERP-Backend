# Call Spring directly so Chrome DevTools → Network shows the real URL (e.g. http://localhost:8080/auth/login).
# Requires CORS on the backend (already configured for http://localhost:5173).
VITE_API_BASE_URL=http://localhost:8080
