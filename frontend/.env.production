# Same host as API (e.g. Spring serves static + REST): leave empty — browser calls /auth/login on same origin.
# If the UI is on another domain than the API, set the API origin, e.g.:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
